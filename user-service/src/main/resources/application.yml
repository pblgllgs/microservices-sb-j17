server:
  port: ${PORT:0}
spring:
  application:
    name: users-ws
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  datasource:
    url: jdbc:mysql://localhost:3306/db_users
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    database: mysql
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.uuid}}

gateway:
  api: 127.0.0.1

token:
  expiration-time: 3600000
  secret: 77m3PLQi78JgU6FGTBLpN6401bA07ZM3GOVPnj5bgtUW314o4C

login:
  url:
    path: /users/login